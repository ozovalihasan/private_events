<% if @attend_events %>
<div>
    <% @events.each do |event| %>
    <%= event.date %> |
    <%= event.title %>
    <br>
    <% end %>
</div>
<div>

    <% @attend_events.each_with_index do |attend_event, index| %>
    <%= attend_event.title %> <br>
    <% end %>

</div>
<% end %>